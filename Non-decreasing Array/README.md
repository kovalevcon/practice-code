# Non-decreasing Array

Source: [Link](https://leetcode.com/problems/non-decreasing-array/)

## Task:

Given an array with `n` integers, your task is to check if it could become non-decreasing by modifying at
most `1` element.

We define an array is non-decreasing if `array[i] <= array[i + 1]` holds for every `i` (1 <= i < n). 

**Sample Input**

```
[4,2,3]
```

**Sample Output**
```
True
```

## Tags:

* Fundamentals
* Algorithms
* Arrays

## Solution:

* **PHP 7** language in `non-decreasing-array.php`.
* **Python 3** language in `non-decreasing-array.py`.
* **JavaScript ES6** language in `non-decreasing-array.js`.
* **Go** language in `non-decreasing-array.go`.